
import React from 'react';

const BackgroundImage = ({ }) => {
    return (
        <div id={'background-image'} className=' fixed top-0 left-0 z-[-1] w-screen h-screen bg-cover bg-no-repeat app-background dark ' />
    );
};

export default BackgroundImage;
